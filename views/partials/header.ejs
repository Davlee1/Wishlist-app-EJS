<div class="center">
<h1 class="title">Wishlist Application</h1>

<% if (user) { %>
   <div class="corner-block">
    <p>Hello <%= user.name %>!</p>
    
      <form method="POST" action="/sessions/logoff">
        <input type="hidden" name="_csrf" value="<%= _csrf %>" />
          <div class="row">
            <a class="button-home" href="/"><img class="icon" src="/img/home.png" alt="Home" ></a>
            <button class="button-logoff">Logoff</button>
          </div>
      </form>
    </div>
   
<% } else{ %>  
   
      <a class="button-home-not-logged-in" href="/"><img class="icon" src="/img/home.png" alt="Home" ></a>
    
<% } %>

<div>
<% if (errors) {
    errors.forEach((err) => { %>
      <div class="error">
        Error: <%= err %>
      </div>
    <% })
  } %>
  <% if (info) {
    info.forEach((msg) => { %>
      <div class="info">
        Info: <%= msg %>
      </div>
    <% })
  } %>
</div>